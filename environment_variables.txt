# Production Environment Variables for Tropical Cloud Detection

# Django Settings
DEBUG=False
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=your-domain.onrender.com,localhost,127.0.0.1

# Security Settings
SECURE_SSL_REDIRECT=False
SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https

# CORS Settings
CORS_ALLOWED_ORIGINS=https://your-domain.onrender.com,http://localhost:3000

# Database (SQLite - no additional configuration needed)
# SQLite database will be created automatically

# File Upload Settings
FILE_UPLOAD_MAX_MEMORY_SIZE=524288000
DATA_UPLOAD_MAX_MEMORY_SIZE=524288000

# Database (PostgreSQL for production)
DATABASE_URL=postgresql://username:password@host:port/database

# Optional: AWS S3 for media files (if using S3 storage)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=us-east-1

# Optional: Additional settings
# DJANGO_SETTINGS_MODULE=cloud_detection_portal.settings

# NOTE: 
# 1. Generate a new SECRET_KEY using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 2. Replace 'your-domain.onrender.com' with your actual Render domain
# 3. The DATABASE_URL will be automatically provided by Render when you add a PostgreSQL database 